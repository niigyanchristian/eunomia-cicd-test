# Hello World Node.js Server

Build a simple HTTP server using Node.js (no frameworks).

## Requirements

1. Create a file `server.js` that starts an HTTP server on port 3000
2. The server responds with "Hello, World!" to all GET requests
3. Create a `package.json` with name "hello-server" and a start script
